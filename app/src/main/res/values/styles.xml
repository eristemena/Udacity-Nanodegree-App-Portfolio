<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="menuTitle" parent="android:Widget.TextView">
        <item name="android:textColor">#01579B</item>
        <item name="android:textSize">24sp</item>
    </style>
    <style name="button1" parent="android:Widget.Button">
        <item name="android:background">@drawable/button1</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_marginBottom">10dp</item>
    </style>
    <style name="button2" parent="android:Widget.Button">
        <item name="android:background">@drawable/button2</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_marginBottom">10dp</item>
    </style>

</resources>
